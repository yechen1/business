import { useRef, useState } from 'react';
import type { ActionType } from "@ant-design/pro-table"

const user = () => {
  // 表格 ref
  useRef<ActionType>()
  const state = {
  };
  const action = {}
  return {
    state,
    action,
  };
};

export default user;
